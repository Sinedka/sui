{BaseFunctions, console, ...}@inputs :
let:
  onWindowClose = console.out "Hello world";
in:
{
  BaseObject = BaseFunctions.BaseObject {
    style = 
  };

  Properties = {
    OnWinClose = onWindowClose
    
  }
}
